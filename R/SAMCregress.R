#' Linear Regression with SAMC
#' 
#' 
#' @export
SAMCregress <- function(y, X, add.coeff=TRUE, options=list()){
  # > myfunc <- function(W){
  #   +     
  #     +     myfunc2=function(X){
  #       +         Y=W%*%X
  #       +         return(Y)
  #       +     }
  #     +     return(myfunc2)
  #     + }
}